# S1.2 — Infra checklist + smoke

> **Última actualización:** 2026-08-05  
> **Proyecto app:** Vercel `citabella` / URL prod `https://citabella-eight.vercel.app`  
> **Proyecto marketing:** `gotacheck-marketing` / `https://gotacheck.app` (fuera de este sprint)  
> **Regla:** no borrar env vars existentes de founders; solo completar faltantes + Redeploy.

## 1. Supabase (cloud)

| Ítem | Estado / acción |
|------|-----------------|
| Migraciones 001–015 | ✅ Verificado 2026-08-05 — `docs/MIGRATIONS_CHECKLIST.md` |
| Plan Free vs Pro | 🟡 Free OK para piloto. **Pro el día del primer cobro** (backups diarios) — `ROUTE_GOTACHECK.md` §8 |
| Backups | En Free: exports manuales si hay cambio fuerte. Pro: backups automáticos |
| Storage buckets | `comprobantes` (privado), `logos-salon` (público) — no recrear |
| Auth providers | Email/password (MVP) |

### Auth → URL configuration (hacer en Dashboard)

Supabase → **Authentication → URL Configuration**:

| Campo | Valor recomendado |
|-------|-------------------|
| Site URL | `https://citabella-eight.vercel.app` |
| Redirect URLs | `https://citabella-eight.vercel.app/**` |
| | `http://localhost:3004/**` |
| | (futuro) `https://app.gotacheck.app/**` |

Guardar. Sin esto, recovery/magic links pueden apuntar mal.

## 2. Vercel — proyecto **app** (no marketing)

Settings → Environment Variables → **Production** (y Preview si usas PRs):

| Variable | ¿Requerida? | Valor / nota |
|----------|-------------|--------------|
| `NEXT_PUBLIC_SUPABASE_URL` | Sí | Proyecto Supabase |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Sí | Anon key |
| `SUPABASE_SERVICE_ROLE_KEY` | Sí | Service role (server only) |
| `NEXT_PUBLIC_SITE_URL` | Sí (S1.2) | `https://citabella-eight.vercel.app` — links de reserva en dashboard |
| `VAPID_PUBLIC_KEY` | Sí (push) | Generar con `node scripts/generate-vapid-keys.mjs` **una vez** |
| `VAPID_PRIVATE_KEY` | Sí (push) | Mismo run del script |
| `VAPID_SUBJECT` | Sí (push) | ej. `mailto:tu@correo.com` |
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` | Opcional | Mismo valor que `VAPID_PUBLIC_KEY` (legacy) |

Después de cambiar env: **Redeploy** del proyecto app.

### Generar VAPID (si faltan en Vercel)

```bash
cd /Users/ch3/Desktop/CITABELLA   # root de la app
node scripts/generate-vapid-keys.mjs
```

Copia las 3 líneas a Vercel **y** a `.env.local`. No regeneres si ya hay keys en prod (invalidaría suscripciones push existentes).

### Hallazgo local (2026-08-05)

| Variable local | Estado |
|----------------|--------|
| Supabase trio | SET |
| `NEXT_PUBLIC_SITE_URL` | MISSING → conviene agregar (local + Vercel) |
| `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` | SET |
| `VAPID_PUBLIC_KEY` | MISSING (sí hay `NEXT_PUBLIC_VAPID_PUBLIC_KEY` — la app lo acepta como fallback) |

Recomendación: en local, copia el valor de `NEXT_PUBLIC_VAPID_PUBLIC_KEY` también a `VAPID_PUBLIC_KEY` para evitar confusiones.

## 3. Smoke (prod)

### A. Público (automatizable)

```bash
node scripts/smoke-s1.2-public.mjs
```

Esperado: login + `/reservar/salon-tutis` + `/reservar/galaxy-barberia-infantil` → HTTP 200.

**Verificado 2026-08-05:** las tres URLs responden 200.

### B. Manual (dueño / operador) — firmar

Usar cuenta founder o demo **sin** borrar datos reales de clientas:

| # | Prueba | OK? |
|---|--------|-----|
| 1 | Login en `/login` | ☐ |
| 2 | Dashboard carga; citas del **propio** salón | ☐ |
| 3 | Link reserva en UI apunta a `citabella-eight…/reservar/{slug}` (o SITE_URL) | ☐ |
| 4 | Reserva de prueba en link público (servicio corto) | ☐ |
| 5 | Comprobante sube / cita en `pendiente_validacion` | ☐ |
| 6 | Admin ve pago en `/pagos` y puede validar o cancelar | ☐ |
| 7 | `/ajustes` → activar notificaciones → “Probar” push en este dispositivo | ☐ |

Si el paso 7 falla: revisar VAPID en Vercel + Redeploy + iPhone requiere PWA en inicio.

### C. Marketing (sanity, no bloquea S1.2)

- [x] `https://gotacheck.app` / `www` → landing (2026-08-04)
- [ ] `NEXT_PUBLIC_APP_URL` en proyecto marketing = `https://citabella-eight.vercel.app/login`

## 4. Criterio Done S1.2

- [x] Checklist documentado  
- [x] Smoke público 200  
- [x] Auth redirect URLs (prod + localhost `/auth/callback`) — operador 2026-08-05  
- [x] Env / push operativo en prod (prueba en dispositivo founder Tutis) — operador 2026-08-05  
- [x] Smoke manual confirmado por operador (login + push OK) — 2026-08-05  

**S1.2 = ✅ cerrado.**

## 5. Qué no es S1.2

- Script genérico de alta → **S1.3**  
- Aislar Belleza Luna / smoke RLS A≠B → **S1.4**  
- Cambios a `gotacheck.app` marketing  
- Subir a Supabase Pro (solo documentar disparador)
